//abaixo, criaremos uma função simples para exemplificar
//a tipagem de retorno é feita de maneira simples, apenas usando '=>'
function exemplo(x, y) {
    return x + y;
}
let exemplo1; //aqui, tipamos o retorno como number usando '=>'
