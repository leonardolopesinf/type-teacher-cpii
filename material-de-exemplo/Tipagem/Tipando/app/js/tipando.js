//Primeiro temos que escolher se usamos "var" ou "let" e um nome pra variável
// Vamos escolher uma variável com nome de idade , e vamos usar o let que é mais comum
// Com a variável já declarada vamos escolher um tipo, como pegamos idade vamos colocar number
// Com o tipo number já podemos atribuir um valor a variável que aceito o tipo number , no caso são números
let idade = 19;
// Vamos escolher mais um para escolher um tipo para fixar
// Vamos fazer uma lista então uma boa opção é o tipo array
let lista = [1, 2, 3];
// Agora podemos colocar para aparecer essas variáveis para termos certeza que funciona.
document.write('Idade: ' + JSON.stringify(idade) + '<br>');
document.write('Lista: ' + JSON.stringify(lista) + '<br>');
